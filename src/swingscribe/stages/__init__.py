"""Pipeline stages: pure functions (Document, Config) → Document.

Stages never import each other; shared types live in swingscribe.model.
All modules here are empty stubs until their milestone (plan §7).
"""
